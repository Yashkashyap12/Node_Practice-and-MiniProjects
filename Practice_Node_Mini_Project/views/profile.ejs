<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex float-right">
            <a class="bg-red-500 rounded-lg px-3 py-1 text-sm" href="/logout">Logut</a>
        </div>
        <h3 class="text-3xl mt-5">Hello, <%=user.name%>👋</h3>
        <h5 class="mb-2">you create a new post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="what's on your mind ?" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-lg" name="content" id=""></textarea>
            <input class="px-3 py-2 bg-blue-400 rounded-lg block mt-2 w-40 text-sm" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-2xl text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-4">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-3 rounded-lg bg-zinc-800 p-4 text-md border-[1] border-zinc-500">
                        <h4 class="mb-2 text-blue-400">@<%= user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="mt-2 inline-block"><%= post.like.length%> likes</small>
                        <div class="btns flex gap-4">
                            <a class="text-blue-400 mt-2" href="/like/<%=post._id%>">
                                <%= post.like.indexOf(user.userid) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-400 mt-2" href="/edit/<%=post._id%>">edit</a>
                        </div>
                    </div>
                    <% })%>
                </div>
            </div>
        </div>
    </div>
</body>
</html>




