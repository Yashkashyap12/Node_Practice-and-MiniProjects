<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=h1, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>

<body class="h-full w-full bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500" href="/">Back To Home</a>
        <h1 class="text-2xl mb-5 mt-10 tracking-tighter">All Users</h1>
        <div class="users flex gap-12 flex-wrap">
            <% if(users.length > 0){%>
                <%users.forEach(function(user){%>
                    <div class="user w-80 p-5 bg-zinc-600 rounded-lg">
                        <div class="w-full h-52 rounded-lg bg-zinc-500 overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image%>" alt="">
                        </div>
                        <h3 class="text-2xl mt-3 tracking-tighter"><%=user.name%></h3>
                        <h5 class="text-blue-300"><%=user.email%></h5>
                        <div class="flex gap-3">
                            <a class="rounded-lg bg-yellow-500 text-white px-1 py-1 mt-5" href="/edit/<%= user.id%>">Edit User</a>
                            <a class="rounded-lg bg-red-500 text-white px-1 py-1 mt-5" href="/delete/<%=user.id%>">Delete User</a>
                        </div>
                    </div>
                <%})%>

           <% } else{ %>
            <h3 class="text-zinc-500 text-xl">No Users Yet</h3>
          <% } %>
        
        </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>

</html>